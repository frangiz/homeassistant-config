  - platform: min_max
    name: Mean temp second floor
    type: mean
    entity_ids:
      - sensor.office_temp
      - sensor.bedroom_temp

  - platform: min_max
    name: Mean temp first floor
    type: mean
    entity_ids:
      - sensor.livingroom_temp
      - sensor.hallway_temp